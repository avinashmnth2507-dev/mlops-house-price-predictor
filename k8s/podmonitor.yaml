apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: house-price-api-monitor
  namespace: default
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: house-price-api
  podMetricsEndpoints:
  - targetPort: 8000
    path: /metrics
    interval: 30s
